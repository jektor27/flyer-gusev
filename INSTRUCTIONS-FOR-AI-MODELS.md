# Инструкция по работе с проектом «Флаер Гусев» (gusev-flyer.ru)

> **Для кого:** любая ИИ-модель, продолжающая работу над этим проектом вместо предыдущей.
> Этот файл заменяет весь контекст прошлых сессий. Прочитайте его полностью **до начала работы**.

---

## 1. Об ассистенте и пользователе

- **Имя ассистента (обязательно):** Jasta, женский род, дружелюбный профессиональный тон, на русском языке.
- **Пользователь:** miroz. Платформа: Windows 10/11. Основная рабочая папка ассистента: `C:\Users\miroz\Documents\New OpenCode Project`.
- **Правила общения:** кратко и по существу; предлагать решения, а не просто описывать проблему; проверять информацию инструментами, а не угадывать.

## 2. Проект в двух словах

Статический сайт-каталог магазинов города Гусев (Калининградская область). Хостинг — **GitHub Pages**, домен — **gusev-flyer.ru** (прописан в `CNAME`). Файлы — чистый HTML/CSS/JS без сборщика. Изменения публикуются **только через git push**.

- **Путь проекта:** `D:\Jastas\projects\flyer-gusev`
- **Git remote:** `https://github.com/jektor27/flyer-gusev.git`, ветка `main`.
- **Структура:** `index.html` (главная) + страницы категорий (`odezhda.html`, `mebel.html` и др.), `category.html?cat=slug` (динамическая категория по URL), `js/main.js` (все данные и логика), `css/style.css`, блог в `blog/`.

## 3. Критично: как публиковать изменения

GitHub Pages обновляется **только после push**. Локальные правки пользователь не видит.

```bash
cd D:\Jastas\projects\flyer-gusev
git add -A
git commit -m "fix: короткое описание"
git push https://jektor27:<TOKEN>@github.com/jektor27/flyer-gusev.git main
```

> ⚠️ `<TOKEN>` — персональный токен GitHub (PAT) пользователя. Токен живёт **только в памяти ассистента/окружении**, в репозиторий его писать ЗАПРЕЩЕНО — GitHub блокирует push с секретами (push protection). При публикации замените `<TOKEN>` на актуальный токен.
> После пуша CDN GitHub Pages отдаёт свежий файл не сразу — проверять онлайн лучше через 30–60 секунд и с параметром кэша (`?v=<timestamp>`).

### Локальная проверка
```bash
python -m http.server 8765 --directory D:\Jastas\projects\flyer-gusev
```
Затем открыть `http://localhost:8765/index.html` в Playwright/браузере. После проверки сервер остановить (найти процесс: `Get-NetTCPConnection -LocalPort 8765 -State Listen`).

## 4. Архитектура и ключевые правила

### 4.1. Единая шапка и подвал
- Шаблон живёт в **`navigation.html`** — это НЕ страница, это фрагмент для копирования.
- На каждой странице шапка и футер должны быть **одинаковыми** и совпадать с `navigation.html`.
- Шапка: `<header class="site-header">` → `.container` → `.logo` (иконка ФГ + текст «Флаер**Гусев**») + `nav.nav` (5 ссылок: Главная, Категории, Тарифы, Блог, Контакты) + `button.menu-btn` (бургер).
- Меню сворачивается в бургер на `max-width:1140px` (CSS в `style.css`), переключает `js/main.js → toggleMenu()`.
- ⚠️ **Запрещено** делать шапку «своей» на отдельной странице (inline-шапка с `nav.main-nav` без бургера). Из-за этого уже был баг: логотип прилипал к «Главная», не было адаптива.

### 4.2. Логотип и фавикон (текущий дизайн)
- Лого-иконка: **буквы «ФГ»** — «Ф» белая `#ffffff`, «Г» красная `#e94560`, на тёмно-синей плашке `#16213e` (CSS-класс `.logo-icon`).
- Inline SVG (вставляется в каждую страницу в `.logo-icon`):
```html
<svg viewBox="0 0 100 100" width="34" height="34" aria-hidden="true"><text x="36" y="54" font-family="Segoe UI, Arial, sans-serif" font-size="72" font-weight="900" fill="#ffffff" text-anchor="middle" dominant-baseline="central">Ф</text><text x="83" y="54" font-family="Segoe UI, Arial, sans-serif" font-size="72" font-weight="900" fill="#e94560" text-anchor="middle" dominant-baseline="central">Г</text></svg>
```
- Файлы фавикона: `favicon.svg` (исходник), `favicon.png` (32×32), `favicon.ico` (16/32/48), `apple-touch-icon.png` (180×180). Ссылки на них прописаны в `<head>` всех страниц.
- ⚠️ Не заменять логотип на эмодзи (🫎/📋) — это был провал №1: эмодзи превращается в «иероглиф» на 16px.
- ⚠️ Если модель умеет видеть изображения: перед генерацией логотипа читайте файл `DESIGN-BRIEF-FAVICON-LOGO.md` в корне проекта и показывайте эскизы пользователю на согласование.

### 4.3. Палитра бренда (CSS-переменные в `css/style.css:7-22`)
| Роль | HEX |
|---|---|
| Основной тёмный фон | `#0a0a1a` / `#0f0f23` |
| Тёмно-синий (панели) | `#16213e` |
| Акцент (красный) | `#e94560` |
| Акцент светлый | `#ff6b6b` |
| Золото | `#d4af37` |
| Светлый текст | `#ffffff` |
| Серый текст | `#8892b0` |

Градиент бренда: `linear-gradient(135deg, #e94560, #d4af37)`.

### 4.4. Данные магазинов — единый источник
- Вся информация о магазинах и категориях — **в `js/main.js`** в константах `shopsData` и `categories` (массивы/объекты с id, названиями, адресами, телефонами, описаниями, продуктами, рейтингами, категориями).
- ⚠️ Добавлять/менять магазин ТОЛЬКО в `shopsData`. Отдельные страницы категорий читают эти же данные (или генерируются из них).
- Есть генератор страниц `create_landing_pages.py` — если меняется разметка карточек, им удобно пересобирать. ⚠️ Скрипт перенесён в `D:\Jastas\projects\flyer-gusev-work\scripts\create_landing_pages.py` (вне git-репо).

### 4.5. «Популярные запросы» на главной
- Сортировка по просмотрам категорий, хранящимся в **localStorage** (`flyer_cat_clicks`).
- Функции в `js/main.js`: `getCategoryClicks`, `registerCategoryClick`, `loadPopularQueries`.
- Не удалять localStorage-логику — это осознанная фича.

### 4.6. Рейтинг магазинов
- `rating` в `shopsData` — рейтинг, показываемый на карточках. Не «живой», а редактируемый вручную.
- Карточки могут подсчитывать клики по магазинам (есть функция рейтинга по кликам — проверяйте, что она не конфликтует с ручным рейтингом).

### 4.7. Ссылки на внешние сайты — ТОЛЬКО по тарифам (жестко)
- **Тарифы** (`tariffs.html`): базовая карточка 0 ₽ = название, адрес, телефон — **БЕЗ ссылки на сайт**. Ссылка на сайт магазина — **только** в тарифах «Приоритетная» (990 ₽) и «Топ» (1990 ₽), т.е. только если тариф реально куплен.
- ⚠️ **НИКАКОГО увода трафика на внешние сайты бесплатно**: если тариф не куплен — никаких ссылок `shop-site`/`website` на сайты магазинов.
- Проверять **везде**, не только на карточках: карточки на главной, карточки в категориях, подвал/шапка, **статьи блога** (`blog/*.html`, `article-*.html`), любые упоминания URL магазинов в тексте.
- При добавлении любого магазина или упоминания его сайта — **сначала сверять с базой тарифов** (кто заплатил), и только потом вносить ссылку.
- Рендер ссылки сайта в динамике `js/main.js` — через отдельное поле `website`, которое должно присутствовать исключительно у платных карточек. У базовых карточек поля `website` быть не должно.

## 5. Структура страниц (критично для правок)

- **`index.html`** — главная: герой, категории, «Популярные запросы», магазины.
- **`category.html?cat=slug`** — универсальная страница категории (slug: mebel, produkty, stroymaterialy, odezhda, tehnika, byt, apteka, avto, kanctovary, cafe).
- **Отдельные статичные страницы категорий** (`mebel.html`, `odezhda.html`, ...) — дублируют `category.html?cat=...`. При правке данных/разметки проверяйте ОБА варианта.
- **`tariffs.html`** — тарифы на размещение. **`contacts.html`** — контакты. **`blog.html`** и `blog/` — блог. **`moose.html`** — страница «О лосе» (символ города).
- **`categories.html`** — обзор всех категорий.
- Служебные НЕ публикуемые файлы проекта перенесены (08.09.2026) из git-репо в `D:\Jastas\projects\flyer-gusev-work\` (вне git, на сайте их нет):
  - `scripts/` — рабочие скрипты: `audit_site.py`, `bing_seo.py`, `create_landing_pages.py`, `test_vk_token.py`, `verify_jsprav.py`.
  - `docs/` — черновики/справки: `audit_report.md`, `SEO_DICTIONARY.md`, `avito-*.md`, `LOCAL-SEO-PLAN.md`, `DESIGN-BRIEF-FAVICON-LOGO.md`, `screenshot-school.png`.
  - `preview/` — превью-страницы/эскизы: `navigation.html`, `breadcrumbs.html`, `ozon-preview.html`, `ozon-points-preview.html`, `shop-premium-preview.html`, `icon-*.html`/`icon-*.png`.
  - `media/` — медиа/дизайны: `design-preview/`, `videos/`, `vk-presentation/`.
  - `vk/` — графика для ВК (`vk-*.png`, `vk-*.html`, `vk-cover*`, `vk-avatar*`, `vk-post-preview.png`).
  - В корне репо остаются ТОЛЬКО публичные страницы, `css/`, `js/`, `img/`, `blog/`, favicon, og-image, robots, sitemap, ключи IndexNow (`39ce...txt`, `a434...txt` — НЕ удалять), `README.md`, `deploy.ps1`, `INSTRUCTIONS-FOR-AI-MODELS.md`.
  - Файл `deploy.ps1` — В КОРНЕ репо (завязан на `deploy_flyer`), не переносить.

## 6. Типичные задачи и как их делать

1. **Починить вёрстку/шапку** → сверить страницу с `D:\Jastas\projects\flyer-gusev-work\preview\navigation.html` (эталон меню; в репо его нет), поправить, проверить на 4 ширинах (1280 / 1140 / 800 / 400) через Playwright, проверить, что на ≤1140px меню сворачивается в бургер и нет горизонтального скролла.
2. **Добавить магазин** → добавить объект в `shopsData` (js/main.js), проверить отображение на главной и в категории, задеплоить.
3. **Поменять фавикон/логотип** → изменить `favicon.svg`, сгенерировать PNG/ICO (рендер SVG через Playwright → Pillow), обновить inline SVG во ВСЕХ html и в `blog/*.html` (в `create_landing_pages.py` из `flyer-gusev-work\scripts\`), проверить читаемость на 16px.
4. **SEO** → для продвижения личного бренда «Пряжников Александр Иванович» есть справочники `D:\Jastas\projects\flyer-gusev-work\docs\SEO_DICTIONARY.md` и `LOCAL-SEO-PLAN.md` (в репо их нет), а на сайте — `sitemap.xml`, `robots.txt`; следовать им.
5. **Проверить позиции/robots.txt в Яндекс.Вебмастере** → вход через профиль `C:\Users\miroz\AppData\Local\Temp\opencode\wm-profile` (Playwright, storage_state.json). Рабочий URL анализа robots.txt: `https://webmaster.yandex.ru/site/<site>/tools/robotstxt/` (пути `/tools/robots/`, `/tools/`, `/robots/`, `/checkrobots/` дают 404 — НЕ тратить на них время). Проверенные результаты (08.09.2026): оба сайта — 0 ошибок, sitemap чистые, дубли `category.html?cat=*` запрещены `Disallow: /category.html` и уходят из индекса сами.

## 7. Чек-лист перед сдачей работы (ВСЕГДА)

- [ ] Изменения проверены локально (`python -m http.server 8765` + браузер/Playwright), а не «на глаз».
- [ ] Запущена проверка структуры: нет ссылок на несуществующие файлы, нет оставшихся эмодзи в логотипе, нет inline-шапок `main-nav`.
- [ ] **Сверка тарифов:** ни на карточках, ни в статьях/блоге нет ссылок (`shop-site`/`website`/URL в тексте) на сайты магазинов без купленного тарифа.
- [ ] Закоммичено и запушено (см. п.3).
- [ ] Проверено онлайн на `https://gusev-flyer.ru` (с параметром `?v=<timestamp>` против кэша).
- [ ] Если менялся дизайн — согласование с пользователем ДО деплоя (он сам смотрит PNG-эскизы в `D:\Jastas\projects\flyer-gusev-work\media\design-preview\`).
- [ ] Локальный сервер остановлен.

## 8. Частые ошибки прошлых сессий (НЕ повторять)

1. Эмодзи в качестве фавикона/логотипа → «иероглиф» в вкладке браузера.
2. Своя шапка на одной странице вместо общей → разъезд логотипа и меню, нет бургера.
3. Замена во всех HTML, кроме `blog/*.html` и генератора `create_landing_pages.py` (в `flyer-gusev-work\scripts\`) → остаются старые иконки.
4. Проверка только локально, без пуша → пользователь не видит результат.
5. Правка данных магазинов в разметке страниц вместо `shopsData` → рассинхрон.
6. Проверка кэшированного онлайн-файла → видно «старую» версию, хотя push прошёл.
7. **Ссылка на сайт магазина на базовой (0 ₽) карточке** → бесплатный увод трафика на внешний сайт. Случалось: `website` у ПрофСтайл/Хоббит/ЦентрСтрой был в `shopsData`, показывался на главной. Проверять по тарифам обязательно.

## 9. Полезные команды

```bash
# Проверка, что всё закоммичено и запушено
git -C D:\Jastas\projects\flyer-gusev status
git -C D:\Jastas\projects\flyer-gusev log --oneline -5

# Найти процесс локального сервера и остановить
Get-NetTCPConnection -LocalPort 8765 -State Listen

# Найти остатки эмодзи в логотипе
# (поиск по маске <div class="logo-icon">, после которого не идёт <s)
```

## 10. ВАЖНО для следующей модели

- Начинайте с чтения **этого файла** и **`D:\Jastas\projects\flyer-gusev-work\preview\navigation.html`** (эталон меню), затем `js/main.js` (данные), `css/style.css` (переменные), `README.md` (обзор).
- Пользователь настаивает на краткости, русском языке и проверке фактов инструментами.
- Текущий выбранный дизайн логотипа — «ФГ» (Ф белая, Г красная). Если пользователь попросит «другой стиль» — сначала покажите эскизы (лежат в `D:\Jastas\projects\flyer-gusev-work\media\design-preview\`), дождитесь выбора, только потом деплойте.
